package com.hansen.processing.ui.controls;

/**
 * Represents an alignment inside an user interface.
 * @author Florian Hansen
 *
 */
public enum Alignment {
    LEFT, RIGHT, CENTER, TOP, BOTTOM
}